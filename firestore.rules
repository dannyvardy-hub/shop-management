rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only signed-in users (you) can read or write. Since this app only
    // creates one login for you, this keeps your data private.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
